function compressItems(items, items2) {

    items.forEach(function (items) {
        console.log(items);
    });

    items2.forEach(function (items) {
        console.log(items);
    });

}

// BIG O(items + items2)

// BIG O(n + n)